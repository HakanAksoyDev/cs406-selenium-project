[pytest]
# Test discovery
testpaths = tests

# HTML report output (requires: pip install pytest-html)
addopts =
    --html=reports/test_report.html
    --self-contained-html
    -v

# Custom markers
markers =
    smoke: Quick sanity tests that run on every build
    regression: Full regression test suite
